import { StyleSheet } from "react-native";

const commonStyles = StyleSheet.create({
  container: {
    width: "100%",
    height: "100%",
    backgroundColor: "#fff",
  },
});

export default commonStyles;
